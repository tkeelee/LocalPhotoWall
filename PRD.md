开发一个单机版网页【支持电脑打开和手机打开】，实现功能：打开后是高德地图高清2D地图，可切换标准/卫星模式，右下角放大、缩小、切换（pc或手机）图标。左下角功能区【导入照片、加载数据、播放】。

导入照片，在PC【指定目录包含子目录】，或则手机端，从相册导入【可多选、全选】
导入后读取照片全部gps信息并存储
 按照 照片拍摄时间、照片名、哈希值、路径、缩略图 存到和html同目录的 sqllite db文件
同时在高德地图上显示每一张照片，点位上显示缩略图，随地图放大和缩小缩略图也按比例变化，若鼠标指向某个缩略图，则居中弹出高清原图（地图视野不要放大缩小），不要完全覆盖背景地图，点击大图或则点击任意其他位置，则大图消失。

加载数据则可以选择 db 文件把数据库里的照片信息加载，若大图路径不存在，则把缩略图等比例放大5倍即可，不要报错。

若重复导入照片，根据哈希值判断是否需要从新录入【已有的舍弃不录入】

点击播放，则弹出选择开始结束时间，确认播放【可以选择播放速度】，则按照时间范围内的gps信息点，抖动一下缩略图到2倍大小，播放到下一个gps点缩略图，则之前的缩略图恢复。

注意，过程中不要自动放大缩小地图，只需要移动视野保证播放到的缩略图剧中即可。若用户手动点击放大缩小，在遵循通用放大缩小规则基础上，叠加播放规则【即缩略图等比例变大，然后播放抖动时候是变大后的2倍大小】。