开发一个单机版网页【支持电脑打开和手机打开】，实现功能：打开后是高德地图高清2D地图，可切换标准/卫星模式，右下角放大、缩小、切换（pc或手机）图标。左下角功能区【导入照片、加载数据、播放】。

导入照片，在PC【指定目录包含子目录】，或则手机端，从相册导入【可多选、全选】
导入后读取照片全部gps信息并存储
 按照 照片拍摄时间、照片名、哈希值、路径、缩略图 存到和html同目录的 sqllite db文件
同时在高德地图上显示每一张照片，点位上显示缩略图，随地图放大和缩小缩略图也按比例变化，若鼠标指向某个缩略图，则居中弹出高清原图（地图视野不要放大缩小），不要完全覆盖背景地图，点击大图或则点击任意其他位置，则大图消失。

加载数据则可以选择 db 文件把数据库里的照片信息加载，若大图路径不存在，则把缩略图等比例放大5倍即可，不要报错。

若重复导入照片，根据哈希值判断是否需要重新录入【已有的舍弃不录入】

点击播放，则弹出选择开始结束时间，确认播放【可以选择播放速度】，则按照时间范围内的gps信息点，抖动一下缩略图到2倍大小，播放到下一个gps点缩略图，则之前的缩略图恢复。

注意，过程中不要自动放大缩小地图，只需要移动视野保证播放到的缩略图剧中即可。若用户手动点击放大缩小，在遵循通用放大缩小规则基础上，叠加播放规则【即缩略图等比例变大，然后播放抖动时候是变大后的2倍大小】。

---
照片加载7000多就卡死了，有什么好的优化办法没，再不改变缩略图像素和功能的前提下，如何优化性能
全局视野怎么办呢？
---
高德公开瓦片只覆盖国内，海外（亚洲其他区域等）空白；
把海外照片点位附近瓦片下载到 index.html 同级 tiles/ 下，本地读取，不额外启服务、可弹窗提醒授权
---
用户决定暂不用离线下载，要求保留代码、不启用
---
在缩略图查看卡片，右上角位置增加删除小图标【纯图标不带文字】，点击则提醒“将立即从照片墙数据库文件内删除切不可恢复，是否继续”，点击是就删除（从地图上删除且直接从数据库db文件里删除-无需额外点击保存），否则回到卡片页面。注意：区分点击删除、和卡片其他区域普通点击（取消预览）

1、在缓存里记录上一次播放轨迹选择的开始和结束时间，下次打开自动带入上次选择的。2、当在播放的时候，地图上将本次区间内涉及播放的点位和非本次播放点位做下区分【例如播放的蓝色，不播放的红色或橙色？】

播放时候可随时调整播放速度，且可以推动进度条前进或后退到指定位置播放。
---
1、左下脚新增db文件管理功能（图标，提示语：数据库直接管理），点击弹出窗口，可以选中db文件，在数据区域预览数据完整信息，可以导出csv，也可以导入csv覆盖数据库数据。
2、有些照片不带GPS信息，所以要新增补录功能（图标，手动补录）：地图上支持长按某一个地址位置，补充照片墙，弹出补录窗口，经纬度默认带入，可以选择 拍摄时间（若导入的照片有准确的拍摄时间属性，以导入的为准）、选择点位照片，点击保存，则系统自动保存到数据库db文件且同时在地图上更新加载此点【读取照片路径、哈希值，生成缩略图等，若哈希值已存在，禁止保存】【播放期间不能补录，暂停播放、非播放可以补录，如果补录的时间范围不在播放范围，注意处理状态样式】。

照片名、文件夹名列太太宽，且没法调整列宽。一次性展示数据太多，增加分页展示（展示页面、总页数、PC每页10条就行,手机每页5条），增加按照时间段筛选，默认全部【从最小时间到最大】。导入csv文件覆盖后，在页面上方提醒覆盖结果（导入X条，按照哈希值匹配处理后：Y条覆盖，Z条修改，N条删除）

补录行为必须是在地图上已有数据库载入以后，因为补录是确认完就要保存的，且需要哈希值判断是录入还是不让录入。另外在多次加载数据库的以后，发现哈希值存在重复，默认后载入的为准。

鼠标在缩略图弹出的预览框上滑动时候不要按点击处理，现在滑动就关闭了卡片，导致无法指向删除按钮，也没法复制下面gps等信息。另外在删除按钮边上再增加一个编辑按钮，允许编辑文件路径、文件名，其他如经纬度等可查看但不允许编辑。

---
导入照片，也要先判断是否加载数据库（参考补录判断），否则提醒：请先加载数据库文件再导入（导入需写入数据库并按哈希判重）
如果 增量加载数据 选择的目录不存在 photos.db ，则在选择目录后自动生成包含0条数据的数据库文件并自动加载
当载入照片、增量加载数据、补录时候，都直接写入数据库，无需自行点击保存。
现有点击保存功能修改只是实现备份，备份文件格式为 photos_YYMMDDHHMMSS_bak.db
记录下用户上次打开的目录路径，下次提醒用户选择工作空间时候优先使用记忆路径【若可用】
数据库管理进行管理的db文件不能是正在载入的db文件【类似独立管理功能】
按照上述内容，先整理提案，等我确认再开始开发。

保存到数据库不需要促发备份，只有主动点击备份才需要备份

未初始化点击传照片，报：请先加载数据库文件再导入（导入需写入数据库并按哈希判重） 后直接自动调用初始空间去选择目录。补录 那里也一样。都知道错在哪里且知道要做什么，系统要自己做。

